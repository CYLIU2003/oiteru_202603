# --- 子機用（Raspberry Pi）---
# このファイルは子機（Raspberry Pi）で使用されます
# 親機用は requirements.txt を使用してください

# --- HTTP通信 ---
requests>=2.28.0
flask>=3.0.0            # リモート設定変更受信用APIサーバー

# --- システム監視 ---
psutil>=5.9.0           # CPU/メモリ使用率取得

# --- NFC Reader ---
nfcpy>=1.0.4

# --- Raspberry Pi & Motor Control ---
RPi.GPIO>=0.7.1         # Raspberry Pi GPIO control
Adafruit-PCA9685>=1.0.1 # I2C servo driver
pyserial>=3.5           # Serial communication with Arduino
RpiMotorLib>=1.3.0      # 28BYJ-48 + ULN2003AN ステッピングモーター制御 (BYJMotor) - fallback
pigpio>=1.78            # pigpio デーモン操作用 Python ライブラリ (PigpioZero バックエンド)
# gpiozero は Raspberry Pi OS に標準インストール (無ければ: sudo apt install python3-gpiozero)

# --- GUI (オプション) ---
# tkinterは通常Pythonに同梱されていますが、Raspberry Pi OSで不足している場合:
# sudo apt-get install python3-tk